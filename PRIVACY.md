# データと権限

本拡張は `https://www.disneyplus.com/*` のトップレベルページで動作します。再生処理を扱うMAIN-world script、バッジ用の最小限の状態を中継するISOLATED-world script、ツールバー操作を扱うservice workerがあります。作者向けテレメトリー、解析サービスへの送信、広告、Cookie読取API、`tabs` 権限はありません。

## 拡張機能の権限

- `activeTab`: ユーザーが拡張アイコンや右クリックメニューを操作したタブへの一時的なアクセス。対象URLを検査し、Disney+以外では操作しません。
- `scripting`: そのタブのトップフレームで、フルHDの切替またはデバッグUIの開閉という固定コマンドを実行します。任意のコード・URLをページから受け取って実行しません。
- `contextMenus`: 拡張アイコンの右クリックメニューにデバッグUIの開閉項目を追加します。

service workerは対象タブのURLを操作前と再読み込み前に確認します。ページから中継するのはモード名と失敗フラグだけで、バッジの更新にしか使いません。診断本文、作品URL、Cookie、manifest、ライセンス本文を転送・保存しません。ページ起点の通知でタブの再読み込みや設定変更は行いません。

## ページ内で扱うもの

- ページ本来のfetch/XHR要求と応答、再生SDKのセッション設定を、選択したモードに応じて観測・限定変更します。サイト本来の認証済み通信を止めるものではありません。
- manifest本文は候補の選択・集計に使います。診断への出力は寸法・件数・固定された状態などに限定し、manifest本文や署名付きメディアURLは出力しません。
- EME APIの選択・セッション種別・鍵状態の件数・処理の成否を観測します。鍵ID・復号鍵・ライセンス本文を抽出する機能はありません。CDMへの引数はページ内を通常どおり通過します。
- 「環境チェック」はcodec宣言、HDR表示能力、viewport/DPR、モードに応じたEME能力照会を行います。照会できることと再生できることは別です。
- エラー表示はURLと長い値を省略しますが、任意のエラー文字列を完全に匿名化する保証はありません。公開前に確認してください。

## 保存・出力先

| 保存先 | 内容・期間 |
| --- | --- |
| Disney+ originのlocalStorage | `ioridev.disneyplus4k.mode.v1`: 選択したモード（通常フルHDのON/OFFを含む。同originの次回起動でも使用）。`ioridev.disneyplus4k.checkpoint.v1`: 直近の版・モード・固定された処理段階と時刻（最大8件） |
| 同originのsessionStorage | `ioridev.disneyplus4k.once.v0.4.0`: 診断モードの1回限りの開始票。版・モード・**現在のページURL**・作成時刻を含み、次のページ起動で消費。受付期限30秒。期限は保存データの即時削除を意味しない。通常フルHDの開始には使わない |
| 同originのsessionStorage | `ioridev.disneyplus.debug-ui.v1`: そのタブのデバッグUI表示設定（`1`/`0`） |
| ページのメモリ | 接続CDM、処理経過、寸法・時間・フレーム数など。ページ終了まで |
| クリップボード | ユーザーが「診断をコピー」を押した場合のみ診断文を書き込む。自動送信しない |
| ブラウザー/CDMの管理領域 | persistent-licenseを使う実験では、正規のライセンスがCDMによって保存される場合がある。拡張は鍵や保存ライセンスを読み出さず、独自に削除しない |

MAIN worldとサイトのStorageを使うため、**ページのスクリプトから隔離された秘密の保管場所ではありません**。同じページにあるコードは診断DOM・Storageを参照し得ます。旧Helperとの互換性のため保存キー名は変更していません。

## 元へ戻すには

アイコンでOFFにするかデバッグUIで「無変更」を選び、拡張を無効化または削除し、Disney+タブを閉じるか再読み込みします。変更したEdgeフラグも元の値に戻してください。拡張の削除だけではサイトのlocalStorageやCDMのデータが消えるとは限りません。Storageを消す場合は上記のキーだけを対象にしてください。旧版の `ioridev.disneyplus4k.once.v0.3.16` などが残っている場合もあります。サイト全体のデータ削除はログアウトなど別の影響があります。

GitHubへ添付する前に、手動コピーした診断にもアカウント・URL・トークンなどが残っていないか確認してください。HAR、Cookie、ブラウザープロファイル、メモリダンプ、ライセンス/鍵/証明書のバイナリは投稿しないでください。
