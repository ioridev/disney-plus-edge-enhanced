# データと権限

本拡張は `https://www.disneyplus.com/*` のトップレベルページで動作するMAIN-world content scriptです。バックグラウンド処理、作者向けテレメトリー、解析サービスへの送信、広告、Cookie読取API、タブ一覧取得権限はありません。

## ページ内で扱うもの

- ページ本来のfetch/XHR要求と応答、再生SDKのセッション設定を、選択したモードに応じて観測・限定変更します。サイト本来の認証済み通信を止めるものではありません。
- manifest本文は候補の選択・集計に使います。診断への出力は寸法・件数・固定された状態などに限定し、manifest本文や署名付きメディアURLは出力しません。
- EME APIの選択・セッション種別・鍵状態の件数・処理の成否を観測します。鍵ID・復号鍵・ライセンス本文を抽出する機能はありません。CDMへの引数はページ内を通常どおり通過します。
- 「環境チェック」はcodec宣言、HDR表示能力、viewport/DPR、モードに応じたEME能力照会を行います。照会できることと再生できることは別です。
- エラー表示はURLと長い値を省略しますが、任意のエラー文字列を完全に匿名化する保証はありません。公開前に確認してください。

## 保存・出力先

| 保存先 | 内容・期間 |
| --- | --- |
| Disney+ originのlocalStorage | `ioridev.disneyplus4k.mode.v1`: 選択したモード。`ioridev.disneyplus4k.checkpoint.v1`: 直近の実験版・モード・固定された処理段階と時刻（最大8件） |
| 同originのsessionStorage | `ioridev.disneyplus4k.once.v0.3.16`: 1回限りの実験開始票。版・モード・**現在のページURL**・作成時刻を含み、次のページ起動で消費。受付期限30秒。期限は保存データの即時削除を意味しない |
| ページのメモリ | 接続CDM、処理経過、寸法・時間・フレーム数など。ページ終了まで |
| クリップボード | ユーザーが「診断をコピー」を押した場合のみ診断文を書き込む。自動送信しない |
| ブラウザー/CDMの管理領域 | persistent-licenseを使う実験では、正規のライセンスがCDMによって保存される場合がある。拡張は鍵や保存ライセンスを読み出さず、独自に削除しない |

MAIN worldとサイトのStorageを使うため、**ページのスクリプトから隔離された秘密の保管場所ではありません**。同じページにあるコードは診断DOM・Storageを参照し得ます。旧Helperとの互換性のため保存キー名は変更していません。

## 元へ戻すには

「無変更」を選び、拡張を無効化または削除し、Disney+タブを閉じるか再読み込みします。変更したEdgeフラグも元の値に戻してください。拡張の削除だけではサイトのlocalStorageやCDMのデータが消えるとは限りません。Storageを消す場合は上記のキーだけを対象にしてください。サイト全体のデータ削除はログアウトなど別の影響があります。

GitHubへ添付する前に、手動コピーした診断にもアカウント・URL・トークンなどが残っていないか確認してください。HAR、Cookie、ブラウザープロファイル、メモリダンプ、ライセンス/鍵/証明書のバイナリは投稿しないでください。
