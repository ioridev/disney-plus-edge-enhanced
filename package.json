{
  "name": "disney-plus-edge-enhanced",
  "version": "0.4.0",
  "private": true,
  "description": "Disney+ full-HD request toggle and optional diagnostics for Windows Edge. Long-running playback unverified; 4K not achieved.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ioridev/disney-plus-edge-enhanced.git"
  },
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "test": "node scripts/build.mjs --check && node scripts/test.mjs"
  }
}
