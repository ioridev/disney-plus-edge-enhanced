{
  "name": "disney-plus-edge-enhanced",
  "version": "0.5.0",
  "private": true,
  "description": "Disney+ full-HD toggle, Intel-gated 4K selection and diagnostics for Windows Edge. Five-minute 4K playback observed on one Intel iGPU laptop.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ioridev/disney-plus-edge-enhanced.git"
  },
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "test": "node scripts/build.mjs --check && node scripts/test.mjs"
  }
}
